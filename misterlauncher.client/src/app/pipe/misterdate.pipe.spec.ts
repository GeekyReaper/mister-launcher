import { MisterDatePipe } from './misterdate.pipe';

describe('DatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MisterDatePipe();
    expect(pipe).toBeTruthy();
  });
});
