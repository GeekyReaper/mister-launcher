import { JobstateColorPipe } from './jobstate-color.pipe';

describe('JobstateColorPipe', () => {
  it('create an instance', () => {
    const pipe = new JobstateColorPipe();
    expect(pipe).toBeTruthy();
  });
});
