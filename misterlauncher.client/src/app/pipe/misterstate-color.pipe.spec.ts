import { MisterstateColorPipe } from './misterstate-color.pipe';

describe('MisterstateColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MisterstateColorPipe();
    expect(pipe).toBeTruthy();
  });
});
